/* FIROIU Mihnea-Ioan 313CD */

#include <stdio.h>
#include <stdlib.h>
#include "Graph.h"
#include "Heap.h"

void task1() {
    FILE *input = fopen("tema3.in", "r");
    FILE *output = fopen("tema3.out", "w");

    fprintf(output, "%d", 1);

    fclose(input);
    fclose(output);
    return;
}

void task2() {
    FILE *input = fopen("tema3.in", "r");
    FILE *output = fopen("tema3.out", "w");

    fprintf(output, "%d", 2);

    fclose(input);
    fclose(output);
    return;
}
